server:
  port: 8001

spring:
  application:
    name: flight-operations

  threads:
    virtual:
      enabled: true

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:postgres}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      poolName: SpringBootJPAHikariCP
      auto-commit: true
      minimum-idle: 5
      maximumPoolSize: 20
      maxLifetime: 300000
      leak-detection-threshold: 300000
      transaction-isolation: TRANSACTION_READ_COMMITTED
      health-check-properties: {
        connectivityCheckTimeoutMs: 1000,
        expected99thPercentileMs: 10
      }

  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.json

security:
  jwt:
    secret: "AirlineFlightsWorldwideSecretSecurityTokenKeyForJwtValidation2026"
    expiration: 86400
  seed:
    admin:
      email: "admin@airline.com"
      password: "AdminPassword123!"
      name: "System Administrator"
      phone: "+12025550101"
    crew:
      email: "crew@airline.com"
      password: "CrewPassword123!"
      name: "Senior Captain Smith"
      phone: "+12025550102"
    groundstaff:
      email: "groundstaff@airline.com"
      password: "GroundStaff123!"
      name: "Gate Agent Elena"
      phone: "+12025550103"
    customer:
      email: "customer@airline.com"
      password: "CustomerPassword123!"
      name: "John Doe"
      phone: "+12025550104"
