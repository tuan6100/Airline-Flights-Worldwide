package online.anhht.airline.model;

/**
 * Operational flight status enum.
 */
public enum FlightStatus {
    SCHEDULED,
    ON_TIME,
    DELAYED,
    BOARDING,
    DEPARTED,
    ARRIVED,
    CANCELLED
}
